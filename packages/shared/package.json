{
  "name": "shared",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch ./src --ignore-watch ./dist --ignore-watch ./.turbo",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "tsdown": "^0.15.11",
    "typescript": "^5.2.2"
  }
}